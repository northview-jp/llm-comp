{
  "name": "llm-comp",
  "version": "0.1.0",
  "description": "Compare LLM responses (OpenAI / Claude / Gemini) in a tabbed terminal UI",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "llm-comp": "bin/llm-comp.js"
  },
  "files": [
    "bin/",
    "dist/",
    "LICENSE",
    "MODEL_CATALOG.md",
    "README.md",
    "llm-comp.example.yaml"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepare": "npm run build",
    "test": "npm run build && node --test test/*.test.cjs test/**/*.test.cjs"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
